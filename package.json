{
  "name": "flick",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd ./frontend && npm run start",
    "server": "cd ./backend && npm run dev",
    "build": "npm install && env-cmd -f .env npm run build --prefix ./frontend && npm install --prefix ./backend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "env-cmd": "^10.1.0"
  }
}
